E FUND 易方达风格参考
项目问题与解决
长文件并行处理：材料拆分、去重、截断兜底
核心思路：长材料不整份硬塞给模型，而是按页 / 表格区域切分成任务队列；OCR、表格解析、LLM 抽取并行执行，再由合并层去重、校验和
补偿。
并行处理逻辑
文件切分 任务入队 并发抽取 统一合并 去重兜底 最终输出
→ → → → →
按页 / 表格 / 截图区域 OCR 与表格解析并行 多 chunk 调 LLM 按账户 / 证券 / 日期聚 重复、截断、失败补偿 完整表 + 问题清单
合
提速方式 重叠区域设计 去重合并
• OCR / PDF / Table 解析按页并发 • 每个 分块都包含和其相邻区域的重叠部分， • 交易按账户 + 证券代码 + 日期 + 方向 + 金额
• LLM 抽取按 chunk 并发，限制并发数防止 以此来保护同一笔交易不被截断 近似匹配
拥塞 • 预估输出长度，超限自动二次切分 • 持仓按账户 + 证券代码 + 截止日期归并
• 已解析文件做缓存，重复上传不重新 OCR • 同一证据多次命中只保留一条结果
• 先做材料类型识别，只把相关片段送入抽取 • 冲突结果不覆盖，进入复核清单
长材料处理耗时降低约 50%，速度提升一倍以上
串行处理
并行处理
易方达基金 · 内部交流，请勿外传 05 / 16